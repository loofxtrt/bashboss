local seagate = "/mnt/seagate"
local workspace = "/mnt/seagate/workspace"

local coding = workspace .. "/coding/projects"
local godot = workspace .. "/godot/projects"

local bash = coding .. "/scripts/bashboss/bash"

local paths = {
    SEAGATE = seagate,
    WORKSPACE = workspace,
    CODING = coding,
    GODOT = godot,
    BASH = bash,
}

return paths